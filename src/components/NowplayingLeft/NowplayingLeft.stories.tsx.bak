
import NowplayingLeft from './nowplaying-left';

export default {
  title: 'Now Playing Left',
};

export const nowplayingLeft: React.FC = () => {
  return (
    <div style={{ position: 'relative', top: 40 }}>
      <NowplayingLeft songName="song name" artistName="artist name" />
    </div>
  );
};
